apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopnow.fullname" . }}
  labels:
    {{- include "shopnow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopnow.selectorLabels" . | nindent 4 }}
